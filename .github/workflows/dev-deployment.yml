name: Dev Deployment
on:
  workflow_dispatch:
  push:
    branches:
      - dev
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16.16.0'

      - name: Install dependencies and build
        run: npm ci && npm run build

      - name: Deploy to my ❤️
        uses: up9cloud/action-rsync@master
        env:
          HOST: ec2-54-83-113-247.compute-1.amazonaws.com
          KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          TARGET: /
          VERBOSE: true
          USER: ec2-user
          SOURCE: ./*
  
          PRE_SCRIPT: |
            echo start at:
            date -u
          POST_SCRIPT: "echo done at: && date -u"
      # - name: Deploy to my EC2 instance
      #   uses: easingthemes/ssh-deploy@main
      #   with:
      #     SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY}}
      #     SOURCE: "./*"
      #     REMOTE_HOST: "ec2-54-83-113-247.compute-1.amazonaws.com"
      #     REMOTE_USER: "ec2-user"
      #     TARGET: ""
      #     SCRIPT_AFTER: |
      #       export MONGODB_DB_NAME=${{ secrets.MONGODB_DB_NAME }}
      #       export MONGODB_CLUSTER_ADDRESS=${{ secrets.MONGODB_CLUSTER_ADDRESS }}
      #       export MONGODB_USERNAME=${{ secrets.MONGODB_USERNAME }}
      #       export MONGODB_PASSWORD=${{ secrets.MONGODB_PASSWORD }}
      #       export PORT=${{ secrets.PORT }}
